import React from 'react';
import {GridHeader} from './gridHeader';
import {GridContent} from './gridContent';

export const Grid = () => {
    return (
        <div>
            <GridHeader/>
            <GridContent/>
        </div>
    );
};
